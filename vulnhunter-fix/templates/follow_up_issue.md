## Follow-up: Residual Vector — {VULN_ID}

**Parent finding:** {VULN_ID} — see #{PARENT_ISSUE_NUMBER}
**Completeness tier applied:** {COMPLETENESS_TIER}
**CWE:** {CWE}

### Residual Vector

{RESIDUAL_TEXT}

### Context

The parent PR closed the primary vector of {VULN_ID}, but the vector above
remained open under the applied fix's completeness tier
({COMPLETENESS_TIER}). This follow-up tracks that residual until either:

- A subsequent fix closes it (bumping the parent finding to FULL retroactively).
- A risk-acceptance decision is recorded here with the responsible team.
- The scanner re-detects it as a distinct finding, at which point this
  follow-up becomes redundant and can be closed by the automatic
  deduplication pass.

### Suggested Next Step

{SUGGESTED_NEXT_STEP}

### Reproduction

If a reproduction is known from the parent finding's PoC, it is reproduced
here so this issue is actionable in isolation:

{REPRO_BLOCK_OR_NONE}

---

**Labels:** `vulnhunter-followup`, `security`, `{CWE}`
**Auto-generated by:** VulnHunter-Fix delivery renderer

<!-- vulnfix-followup-key: {FOLLOWUP_KEY} -->
